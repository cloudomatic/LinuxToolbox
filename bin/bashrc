pizero_iot_setup() {
  # Setup external USB drive
  #sudo apt-get -y install ntfs-3g
  #sudo mkdir /mnt/drive/
  #sudo chown pi:pi /mnt/drive
  echo sudo mount -t ntfs-3g -o uid=pi,gid=pi /dev/sda1 /mnt/drive
}

java_dev() {
  sudo apt-get update
  if (more /etc/os-release | grep "^ID=" | grep raspbian); then 
    sudo apt-get install -y oracle-java8-jdk 
  fi
  sudo apt-get install -y maven
  
}

cheat_sheet() {
  alias encrypt='echo gpg --output doc.txt.gpg --symmetric doc.txt'
  alias bs='echo -e "\\n\\n---- Bash shortcuts ---------\\n^   a  home   \\n^   e  end\\nalt r  back one word    \\n-----------------------------"'
  alias bc='bs'
}

unalias ls
cheat_sheet

# Note, no xterm customizations were needed here
